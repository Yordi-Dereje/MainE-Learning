@model IEnumerable<Microsoft.AspNetCore.Identity.IdentityRole>
@{
    Layout = "AdminLayout";
    ViewData["Title"] = "Index";
}


<body>

  
              @*   <ul>
                    <li class="nav-item">
                        <a class="nav-link text-dark" asp-area="Identity" asp-controller="Role" asp-action="Index">Roles</a>
                    </li>
                   
                </ul> *@
  
              @*  < br> *@
@*     < br>   < br>   < br>   < br>   < br>   < br>   < br>   < br>   < br>   < br>   < br>   < br>   < br>   < br>   < br>   < br>   < br>   < br>   < br>
<h1>List of Roles</h1> *@

<a asp-action="Create">Create</a>

<table class="table table-striped table-bordered" style="width: 75vw">

    <thead>
        <tr>
            <td>Id</td>
            <td>Name</td>
        </tr>
    </thead>
    <tbody>
        @foreach (var role in Model)
        {
            <tr>
                <td>@role.Id</td>
                <td>@role.Name</td>
            </tr>

        }
    </tbody>
</table>
</body>
